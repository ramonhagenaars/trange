"""
Setup of this project.
"""
from setuptools import setup

setup(name='trange',
      version='0.1.0',
      description='A library for time ranges',
      packages=[
          'trange'
      ],
      zip_safe=False)
